-- Use the local pycharm-dark colorscheme (colors/pycharm-dark.lua) instead of
-- LazyVim's default tokyonight, so nvim matches PyCharm and the terminal.
return {
  { "LazyVim/LazyVim", opts = { colorscheme = "pycharm-dark" } },
}
