# GENERATED by scripts/SovietLockGen.py - do not edit.
# Rewritten before every lock; edit the generator instead.
# panel: 25 rows x 64 cols   layouts: EN,ES,RU
# monitors: eDP-1, HDMI-A-2

# ══ eDP-1  (1920x1080 logical, font 13, clock 20) ══
label {
    monitor = eDP-1
    text = cmd[update:1000] $BIGCLOCK
    color = $ink
    font_size = 20
    font_family = $mono
    position = 0, 352
    halign = center
    valign = center
}

label {
    monitor = eDP-1
    text = cmd[update:60000] $SL --date
    color = $dim
    font_size = 13
    font_family = $mono
    position = 0, 231
    halign = center
    valign = center
}

label {
    monitor = eDP-1
    text = cmd[update:30000] $SL --panel
    color = $ink
    font_size = 13
    font_family = $mono
    position = 0, -96
    halign = center
    valign = center
}

input-field {
    monitor = eDP-1
    size = 416, 20
    outline_thickness = 0
    rounding = 0
    outer_color = rgba(00000000)
    inner_color = rgba(00000000)
    check_color = rgba(00000000)
    fail_color = rgba(00000000)
    font_color = $ink
    font_family = $mono
    dots_text_format = *
    dots_size = 0.64
    dots_spacing = 0.2
    dots_center = false
    fade_on_empty = false
    hide_input = false
    placeholder_text =
    fail_text = <span foreground="##C42B1C" font_size="13pt"><b>НЕВЕРНЫЙ КОД ДОСТУПА · ПОПЫТКА $ATTEMPTS</b></span>
    check_text = <span foreground="##C7A017" font_size="13pt">ИДЁТ ПРОВЕРКА ДОКУМЕНТОВ...</span>
    position = 78, 48
    halign = center
    valign = center
}

label {
    monitor = eDP-1
    text = $LAYOUT[EN      ,ES      ,RU      ]
    color = $ink
    font_size = 13
    font_family = $mono
    onclick = hyprctl dispatch global quickshell:layoutNext
    position = -90, 72
    halign = center
    valign = center
}

label {
    monitor = eDP-1
    text = cmd[update:0] $SL --footer
    color = $dim
    font_size = 13
    font_family = $mono
    position = 0, -430
    halign = center
    valign = center
}

# ══ HDMI-A-2  (2560x1440 logical, font 18, clock 28) ══
label {
    monitor = HDMI-A-2
    text = cmd[update:1000] $BIGCLOCK
    color = $ink
    font_size = 28
    font_family = $mono
    position = 0, 484
    halign = center
    valign = center
}

label {
    monitor = HDMI-A-2
    text = cmd[update:60000] $SL --date
    color = $dim
    font_size = 18
    font_family = $mono
    position = 0, 314
    halign = center
    valign = center
}

label {
    monitor = HDMI-A-2
    text = cmd[update:30000] $SL --panel
    color = $ink
    font_size = 18
    font_family = $mono
    position = 0, -135
    halign = center
    valign = center
}

input-field {
    monitor = HDMI-A-2
    size = 582, 28
    outline_thickness = 0
    rounding = 0
    outer_color = rgba(00000000)
    inner_color = rgba(00000000)
    check_color = rgba(00000000)
    fail_color = rgba(00000000)
    font_color = $ink
    font_family = $mono
    dots_text_format = *
    dots_size = 0.64
    dots_spacing = 0.2
    dots_center = false
    fade_on_empty = false
    hide_input = false
    placeholder_text =
    fail_text = <span foreground="##C42B1C" font_size="18pt"><b>НЕВЕРНЫЙ КОД ДОСТУПА · ПОПЫТКА $ATTEMPTS</b></span>
    check_text = <span foreground="##C7A017" font_size="18pt">ИДЁТ ПРОВЕРКА ДОКУМЕНТОВ...</span>
    position = 109, 63
    halign = center
    valign = center
}

label {
    monitor = HDMI-A-2
    text = $LAYOUT[EN      ,ES      ,RU      ]
    color = $ink
    font_size = 18
    font_family = $mono
    onclick = hyprctl dispatch global quickshell:layoutNext
    position = -126, 96
    halign = center
    valign = center
}

label {
    monitor = HDMI-A-2
    text = cmd[update:0] $SL --footer
    color = $dim
    font_size = 18
    font_family = $mono
    position = 0, -595
    halign = center
    valign = center
}
