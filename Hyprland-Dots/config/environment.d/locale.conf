# Read by the systemd user session, so it reaches every app in the graphical
# session (the bar's clock and calendar included), not just interactive shells.
# German time locale: 24-hour clock and a Monday-first calendar, while LANG
# stays whatever /etc/locale.conf sets.
LC_TIME=de_DE.UTF-8
